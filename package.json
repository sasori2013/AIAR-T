{
  "name": "aiar-t",
  "version": "1.0.0",
  "description": "8thwall.org Image Target AR Project",
  "main": "index.html",
  "scripts": {
    "start": "serve .",
    "build": "if [ -d public ]; then rm -rf public; fi && mkdir public && cp -rv index.html main.js style.css config.json targets img ar-engine public/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
